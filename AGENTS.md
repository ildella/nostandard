# CLAUDE.md

## Project Overview

**Nostandard** is eslint configuration.
It provides opinionated ruleset around basic eslint, style, vitest and svelte.
All dependencies are and must be optional for the client project.

Checks with `yarn lint` and `yarn test`.
